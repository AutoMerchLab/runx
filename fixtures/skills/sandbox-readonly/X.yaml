skill: sandbox-readonly
version: "0.1.0"
runners:
  default:
    default: true
    type: cli-tool
    command: sh
    args:
      - ./run.sh
    timeout_seconds: 10
    sandbox:
      profile: readonly
      writable_paths:
        - "{{output_path}}"
    inputs:
      output_path:
        type: string
        required: true
