skill: moltbook
version: "0.0.0"
catalog:
  kind: graph
  audience: operator
  visibility: internal
  role: harness-fixture
  part_of: [runx/prior-art]
  execution: read
  completion: runtime_receipt
  requires_adapter: false
  approval: none
runners:
  scan:
    default: true
    type: graph
    graph:
      name: moltbook-fixture
      result_from: [digest]
      steps:
        - id: digest
          tool: data.digest
          inputs:
            value: moltbook-fixture
