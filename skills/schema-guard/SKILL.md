---
name: schema-guard
description: Evaluates schema changes for backward compatibility and enforces policies before publishing.
---

# Schema Guard
Validates `current_schema` against `proposed_schema` based on `compatibility_policy`.

