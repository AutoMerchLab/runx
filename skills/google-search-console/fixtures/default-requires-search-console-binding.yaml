name: google-search-console-default-requires-provider-binding
kind: skill
target: ..
operator_journeys:
  - mode: standalone
    confusors: [google-analytics, organic-growth]
    request: Show page-level Google Search performance for this verified property and date range.
    expected_outcome: Use the live performance runner by default and fail immediately with one provider-binding message when no compatible Search Console grant is configured.
inputs:
  property: sc-domain:example.com
  start_date: "2026-07-01"
  end_date: "2026-07-31"
  dimensions: [query, page]
  search_type: web
  data_state: final
expect:
  status: policy_denied
metadata: { public_skill: google-search-console, source_case: direct-default-preflight, source: skills-fixture }
