name: missing-upstream-source-fails
kind: skill
target: ..
runner: generate
inputs:
  upstream:
    host: github.com
  registry:
    owner: example
expect:
  status: sealed
  steps:
    - inspect
    - reject
  step_outputs:
    reject:
      subset:
        binding_bundle:
          data:
            decision: reject
            files: []
            validation:
              status: hold
metadata:
  public_skill: overlay
  source_case: missing-upstream-source-fails
  source: skills-fixture
