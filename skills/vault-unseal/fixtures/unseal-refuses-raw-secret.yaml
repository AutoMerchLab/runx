name: vault-unseal-refuses-non-reference-material
kind: skill
target: ..
runner: vault-unseal
inputs:
  secret_ref: plaintext-secret-value
  purpose: Sign one request
  ttl: 10m
  scope: { resource: reports, action: export }
  principal: svc/report-exporter
expect:
  status: sealed
  output:
    subset:
      unseal_plan:
        decision: refused
        execution:
          provider_status: not_called
          handle_issued: false
          secret_material_exposed: false
  receipt: { schema: runx.receipt.v1 }
metadata: { public_skill: vault-unseal, source_case: raw-material-refusal, source: skills-fixture }
