name: brand-voice-binds-rules-and-safe-claims
kind: skill
target: ..
runner: voice
operator_journeys:
  - mode: standalone
    confusors: [taste-profile, ghostwrite]
    request: Build a Runx homepage voice packet from these approved and rejected examples.
    expected_outcome: Return a scoped voice packet with evidence-bound rules, safe claims, and explicit anti-patterns.
  - mode: composed
    request: Turn this approved product contract and rejected copy into reusable writing context.
    expected_outcome: Reuse the supplied source material and return a voice packet downstream writers can consume without reinterpreting brand claims.
    prior_evidence:
      - The approved product contract and rejected launch-copy example supplied in the fixture.
    must_not_repeat:
      - Do not rediscover product claims outside the supplied evidence.
      - Do not make downstream writers reconstruct rejected language or applicability.
inputs:
  brand: runx
  channel: homepage
  audience: advanced software builders
  source_material:
    - kind: approved
      label: product contract
      content: Runx receipts bind operator actions to explicit authority evidence.
    - kind: rejected
      label: rejected launch copy
      content: Autonomous everything with no review.
caller:
  answers:
    agent_task.brand-voice-synthesize.output:
      brand_voice_draft:
        decision: ready
        brand: runx
        applicability:
          channels: [homepage]
          audience: advanced software builders
          boundaries: [Do not extend claims beyond admitted evidence.]
        brand_voice:
          voice_principles: [Lead with governed action and proof.]
          vocabulary:
            use: [receipt, authority, evidence]
            avoid: [magic, autonomous everything]
          cadence: [Concise engineering sentences.]
          claim_rules:
            safe: [Runx receipts bind operator actions to authority evidence.]
            requires_proof: [Hosted production readiness.]
            forbidden: [Autonomous everything with no review.]
          channel_adjustments: [Homepage copy can be concise but still evidence-bound.]
        evidence_bindings:
          - rule: Lead with governed action and proof.
            source_refs: [source:1]
          - rule: Runx receipts bind operator actions to authority evidence.
            source_refs: [source:1]
        redactions: []
        stop_conditions: [Require new evidence before adding a new factual claim.]
expect:
  status: sealed
  steps: [index, digest, synthesize, finalize]
  step_outputs:
    finalize:
      subset:
        brand_voice_packet:
          data:
            decision: ready
            receipt_notes: { authority: context-only, mutation: false }
            validation: { status: pass }
  receipt: { schema: runx.receipt.v1 }
metadata: { public_skill: brand-voice, source_case: bound-voice, source: skills-fixture }
