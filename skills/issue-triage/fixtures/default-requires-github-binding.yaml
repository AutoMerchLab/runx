name: issue-triage-default-requires-github-binding
kind: skill
target: ..
operator_journeys:
  - mode: standalone
    confusors: [issue-intake, github-sync]
    request: Triage issue 241 in runxhq/runx and draft the next helpful maintainer response.
    expected_outcome: Read the issue through the default provider runner and fail immediately with one grant message when no GitHub binding is configured.
inputs:
  repository: runxhq/runx
  issue_number: "241"
  objective: Draft the next helpful maintainer response.
expect:
  status: policy_denied
metadata: { public_skill: issue-triage, source_case: direct-default-preflight, source: skills-fixture }
