name: sandbox-harden-bounds-profile-to-runtime-evidence
kind: skill
target: ..
runner: sandbox-harden
inputs:
  workload:
    image_digest: sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
    class: batch_job
    requirements:
      source_ref: requirements:batch-job:v1
      source_digest: sha256:bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb
      observed_at: "2026-07-17T10:00:00Z"
      network: false
      writable_paths: [tmp/work]
      env_allowlist: [PATH]
      cwd_policy: workspace
      require_enforcement: true
  as_of: "2026-07-17T12:00:00Z"
  baseline:
    network_allowed: false
    allowed_writable_paths: [tmp/work]
    allowed_env: [PATH]
    require_enforcement: true
expect:
  status: sealed
  steps: [plan]
  step_outputs:
    plan:
      subset:
        hardening_profile:
          data:
            decision: ready
            declaration:
              profile: workspace-write
              network: false
              writable_paths: [tmp/work]
              require_enforcement: true
            admission: { status: allow }
            applied: false
            validation: { status: pass }
  receipt: { schema: runx.receipt.v1 }
metadata: { public_skill: sandbox-harden, source_case: bounded-profile, source: skills-fixture }
