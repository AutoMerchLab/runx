name: sandbox-harden-stops-without-workload
kind: skill
target: ..
runner: sandbox-harden
inputs:
  workload: {}
  as_of: "2026-07-17T12:00:00Z"
expect:
  status: sealed
  steps: [plan]
  step_outputs:
    plan:
      subset:
        hardening_profile:
          data:
            decision: needs_more_evidence
            applied: false
            validation: { status: fail }
  receipt: { schema: runx.receipt.v1 }
metadata: { public_skill: sandbox-harden, source_case: missing-workload, source: skills-fixture }
