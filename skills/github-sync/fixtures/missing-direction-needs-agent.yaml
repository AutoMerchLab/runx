name: github-sync-missing-direction-fails-input-contract
kind: skill
target: ..
runner: github-sync
inputs:
  repo: runxhq/runx
  resources: { kind: issues, filters: { limit: 10 } }
  scope: read
expect:
  status: sealed
  output:
    subset:
      sync_plan:
        decision: blocked
        provider_status: not_called
  receipt: { schema: runx.receipt.v1 }
metadata: { public_skill: github-sync, source_case: missing-direction, source: skills-fixture }
