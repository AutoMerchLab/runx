name: github-sync-blocks-nul-thread-body
kind: skill
target: ..
runner: github-sync
inputs:
  repo: runxhq/runx
  direction: push
  resources:
    kind: threads
    filters: { limit: 1 }
    mutations:
      - ref: issues/241/comments
        op: comment
        payload:
          body: "before\0after"
  scope: write
expect:
  status: sealed
  output:
    subset:
      sync_plan:
        decision: blocked
        provider_status: not_called
  receipt: { schema: runx.receipt.v1 }
metadata: { public_skill: github-sync, source_case: nul-thread-body, source: skills-fixture }
