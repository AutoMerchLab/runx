name: github-sync-blocks-null-mutation-fields
kind: skill
target: ..
runner: github-sync
inputs:
  repo: runxhq/runx
  direction: push
  resources:
    kind: issues
    filters: { limit: 1 }
    mutations:
      - ref: issues/241
        op: update
        payload:
          title: null
  scope: write
expect:
  status: sealed
  output:
    subset:
      sync_plan:
        decision: blocked
        provider_status: not_called
  receipt: { schema: runx.receipt.v1 }
metadata: { public_skill: github-sync, source_case: null-mutation-field, source: skills-fixture }
