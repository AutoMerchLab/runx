name: github-sync-plans-push-for-later-approval
kind: skill
target: ..
runner: github-sync
inputs:
  repo: runxhq/runx
  direction: push
  resources:
    kind: issues
    filters: { limit: 1 }
    mutations:
      - { ref: "issue:241", op: add_label, digest: sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa }
  scope: write
expect:
  status: sealed
  output:
    subset:
      sync_plan:
        decision: ready_for_approval
        scope_used: repo:write
        provider_status: not_called
        gates: { approval_required: true, approval_ref: "" }
  receipt: { schema: runx.receipt.v1 }
metadata: { public_skill: github-sync, source_case: bounded-push-plan, source: skills-fixture }
